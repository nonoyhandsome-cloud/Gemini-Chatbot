# 🚀 Setup and Run the Gemini Chatbot App
1. Install Required Software: Make sure these are installed first, Node.js & https://nodejs.org. Choose LTS version. After installing, verify in Command Prompt: node -v & npm -v. You should see version numbers.

2. Install VS Code
   
4. Extract the project 
